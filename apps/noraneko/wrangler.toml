name = "noraneko-f3liz-casa"
compatibility_date = "2025-01-01"
workers_dev = false

main = "src/worker.ts"

# 静的なもの(/, /style.css)は assets のまま。/drops/ と /drops/<uuid>/ だけ、
# その場で dl から組む(src/worker.ts)
[assets]
directory = "./dist"
binding = "ASSETS"

[[routes]]
pattern = "noraneko.f3liz.casa"
custom_domain = true
